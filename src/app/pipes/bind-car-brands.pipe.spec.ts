import { BindCarBrandsPipe } from './bind-car-brands.pipe';

describe('BindCarBrandsPipe', () => {
  it('create an instance', () => {
    const pipe = new BindCarBrandsPipe();
    expect(pipe).toBeTruthy();
  });
});
